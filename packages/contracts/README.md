# Shared contracts

The canonical backend contracts are Pydantic models in `services/api/app/contracts.py`. Phase 1 will generate TypeScript types from OpenAPI and move versioned JSON Schema artifacts here to prevent browser/provider schema coupling.
